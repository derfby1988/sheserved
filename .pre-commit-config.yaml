# Pre-commit hooks — Plan 07 Option E
# Secret scanning via gitleaks (developer convenience; CI is source of truth)

repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
        args: [--config, .gitleaks.toml, --verbose]
