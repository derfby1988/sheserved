---
description: แนวทางการดึงข้อมูลผู้ใช้ (Auth Data Guidelines) ในโปรเจกต์ Sheserved
---

# 🛑 กฎสำคัญ: ห้ามดึงข้อมูลผู้ใช้จาก Supabase Auth โดยตรง

ในโปรเจกต์นี้เรา **ไม่ได้ใช้ Supabase Auth** สำหรับจัดการ User Session โดยตรง แต่เราใช้ `AuthService` แบบกำหนดเองและจัดการผ่าน `ServiceLocator` 

## ❌ สิ่งที่ห้ามทำ (Don't)
ห้ามใช้ `Supabase.instance.client.auth.currentUser` หรือ `_client.auth.currentUser` เพราะค่าจะเป็น `null` เสมอ แม้ผู้ใช้จะ Login แล้วก็ตาม

```dart
// แบบนี้จะทำให้ระบบหา Interaction ไม่เจอ
final userId = _client.auth.currentUser?.id; // ❌ ผิด
```

## ✅ สิ่งที่ต้องทำ (Do)
ให้ดึงข้อมูลผู้ใช้จาก `ServiceLocator` หรือ `AuthService` เสมอ

```dart
// วิธีที่แนะนำ
final userId = ServiceLocator.instance.currentUser?.id; // ✅ ถูกต้อง
```

## 📋 แผนการตรวจสอบ (Checklist)
เมื่อมีการเพิ่มปุ่ม Interaction ใหม่ (Like, Bookmark, Vote, ฯลฯ):
1. ตรวจสอบว่าใน Repository มีการรับ `userId` เป็นพารามิเตอร์หรือไม่
2. ตรวจสอบว่าใน Repository ไม่ได้ใช้ `_client.auth.currentUser` เป็นแหล่งข้อมูลหลัก
3. ในหน้า UI (Presentation) ต้องส่ง `userId` ที่ได้จาก `ServiceLocator` เข้าไปใน Repository
4. หากหน้า Repository จำเป็นต้องใช้ User Identity ภายใน ให้เรียกผ่าน `ServiceLocator.instance.currentUser` เท่านั้น

## ⚠️ ผลกระทบหากทำผิด
หากใช้ Supabase Auth โดยตรง:
- สถานะการกดปุ่ม (เช่น Bookmark) จะแสดงผลไม่ถูกต้อง (เป็นสีขาว/ว่าง) แม้ข้อมูลจะอยู่ในฐานข้อมูลจริง
- ระบบจะแจ้งเตือน "ยกเลิกบันทึก" ทันทีที่กดครั้งแรก เพราะในฐานข้อมูลมี Record อยู่แล้วแต่ UI ดึงออกมาไม่ได้เนื่องจากหา User ID ไม่พบ
